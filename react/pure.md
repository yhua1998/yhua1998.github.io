# 组件的纯度

纯函数/组件

- 相同的输入得到相同的输出
- 组件不会对在渲染前存在的变量或对象进行修改
- 但可以允许本地突变，即函数体内部


## 纯组件的好处

- 可以在不同环境运行，例如服务器上，组件可以在不同环境中运行，由于他们对相同的输入返回相同的结构，一个组件可以为许多用户请求服务。

- 可以跳过输入没有改变的组件来提高性能，因为纯函数总是返回相同的结果，可以安全的进行缓存。

- 纯函数使组件可以在任何时候安全的停止计算，如果渲染一个深层组件树的过程中，一些数据发生变化，可以重新开始渲染，不需浪费时间完成过时的渲染。